
script type="text/babel" src="https://js.stripe.com/v3/" /script
const payment = Stripe('<%= Rails.configuration.stripe[:publishable_key] %>');

payment.redirectToCheckout({
    sessionId: '<%= @session.id %>'
}).then (function (result) {
    console.log(result.error.message);
});